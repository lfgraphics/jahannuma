/**
 * Authentication hooks barrel export.
 * Provides convenient access to all authentication-related hooks.
 */

export { useAuthGuard } from "./useAuthGuard";
export { useSafeAuth } from "./useSafeAuth";

export type { AuthActionType } from "./useAuthGuard";
